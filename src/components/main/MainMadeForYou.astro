---
import { playlists } from "@/lib/data";
import MainItemCard from "./MainItemCard.astro";
---
<div class="flex flex-col gap-6">
  <h3 class="text-2xl font-bold">Hecho para ti</h3>
  <div class="flex flex-wrap gap-5">
    {
      playlists.map((playlist) => (
        <MainItemCard playlist={playlist} />
      ))
    }
  </div>
</div>